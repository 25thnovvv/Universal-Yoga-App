<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniversalYogaApp"
        tools:targetApi="31">
        <activity android:name=".ui.YogaSplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.YogaMainActivity" />
        <activity android:name=".ui.auth.YogaLoginActivity" />
        <activity android:name=".ui.auth.YogaRegisterActivity" />
        <activity android:name=".ui.auth.YogaForgotPasswordActivity" />
        <activity android:name=".ui.course.YogaCourseListActivity" />
        <activity android:name=".ui.course.YogaCourseEditorActivity" />
        <activity android:name=".ui.course.YogaCourseDetailActivity" />
        <activity android:name=".ui.course.YogaClassSessionEditorActivity" />
    </application>

</manifest>